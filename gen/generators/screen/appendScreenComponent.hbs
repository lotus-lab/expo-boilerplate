<Drawer.Screen
   name="{{ properCase screenName }}"
  component={ {{ properCase screenName }} }
  options={
    {title: "{{ properCase screenName }}",
    drawerIcon: ({color}) =>  <DrawerIcon name="code" color={color} />}
  }
/>

{/* // [INSERT NEW SCREEN COMPONENT ABOVE] < Needed for generating screen */}
